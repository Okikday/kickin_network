name: kickin_network_example
description: Example app for kickin_network.
publish_to: 'none'

environment:
  sdk: ^3.9.5
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  kickin_network:
    path: ..

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true